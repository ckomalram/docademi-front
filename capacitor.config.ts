import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.docademi',
  appName: 'docademi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
