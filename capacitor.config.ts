import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'docademi_front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
